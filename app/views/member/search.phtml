<?php
/**
 * @var \Phalcon\Mvc\View\Engine\Php $this
 */
?>

<?php use Phalcon\Tag; ?>

<div class="row">
    <nav>
        <ul class="pager">
            <li class="previous"><?php echo $this->tag->linkTo(["Member/index", "Go Back"]); ?></li>
            <li class="next"><?php echo $this->tag->linkTo(["Member/new", "Create "]); ?></li>
        </ul>
    </nav>
</div>

<div class="page-header">
    <h1>Search result</h1>
</div>

<?php echo $this->getContent(); ?>

<div class="row">
    <table class="table table-bordered">
        <thead>
            <tr>
                <th>Id</th>
            <th>Firstname</th>
            <th>Surname</th>
            <th>Membertype</th>
            <th>Dateofbirth</th>
            <th>Age</th>

                <th></th>
                <th></th>
            </tr>
        </thead>
        <tbody>
        <?php foreach ($page->items as $member): ?>
            <tr>
                <td><?php echo $member->getId() ?></td>
            <td><?php echo $member->getFirstname() ?></td>
            <td><?php echo $member->getSurname() ?></td>
            <td><?php echo $member->getMembertype() ?></td>
            <td><?php echo $member->getDateofbirth() ?></td>
            <td><?php echo $member->getAge() ?></td>
            <td><img class="img-responsive center-block" height="200" width="100" src="data:image/jpeg;base64,<?php echo $member->getMemberpic() ?>"></td>
                <td><?php echo $this->tag->linkTo(["Member/edit/" . $member->getId(), "Edit"]); ?></td>
                <td><?php echo $this->tag->linkTo(["Member/delete/" . $member->getId(), "Delete"]); ?></td>
                <td><?php echo $this->tag->linkTo(["memberimage/new/" . $member->getId(), "Add Images"]); ?></td>
            </tr>
        <?php endforeach; ?>
        </tbody>
    </table>
</div>

<div class="row">
    <div class="col-sm-1">
        <p class="pagination" style="line-height: 1.42857;padding: 6px 12px;">
            <?php echo $page->current, "/", $page->total_pages ?>
        </p>
    </div>
    <div class="col-sm-11">
        <nav>
            <ul class="pagination">
                <li><?php echo $this->tag->linkTo("Member/search", "First") ?></li>
                <li><?php echo $this->tag->linkTo("Member/search?page=" . $page->before, "Previous") ?></li>
                <li><?php echo $this->tag->linkTo("Member/search?page=" . $page->next, "Next") ?></li>
                <li><?php echo $this->tag->linkTo("Member/search?page=" . $page->last, "Last") ?></li>
            </ul>
        </nav>
    </div>
</div>
